#!/usr/bin/env python3
# Status: experimental
# Path: tests/test_night_cycle.py — stub (데이터 없음)
"""Night pipeline tests — all test functions depend on stale experiment data (r1 rotation)
that has been cleaned up. Restore when fresh experiment data is available."""
if __name__ == "__main__":
    import sys
    print(sys.stderr and "Night pipeline: no experiment data available. Run a full pipeline first.")
