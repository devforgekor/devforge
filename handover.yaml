last_checkpoint:
  time: '2026-07-24T17:30:00+00:00'
  summary: 'Fix set complete: nd13-01~06 all resolved. Evidence: context_limit(400) sandwich (200+200) replaces [:300]. Token-based dynamic batch splitting (MAX_BUDGET=7373) prevents ctx overflow. Web-verified: Lost in the Middle (TACL 2024) + EMNLP 2025 confirm sandwich+shorter is optimal. 40-turn regression (v4): 0 errors, 165 new facts, 965 total DB facts. All changes production-ready.'
  total_files: 6
  recent_files:
    source:
      - scripts/lib/model_registry.py
      - scripts/pipelines/extract_verify.py
      - scripts/lib/llm_client/__init__.py
      - scripts/pipelines/extract.py
      - scripts/day_cycle.sh
    config: []
    docs: []
    data: []
    generated: []
  git:
    server:
      branch: main
      unstaged: 0 files changed
      staged: (none)
      untracked_files: 0
  task: null
decisions:
- id: nd13-01
  detail: 'Reranker HTTP 500 root cause: llama.cpp --batch-size 256 too small. Embedding/reranker input tokens must fit within physical batch size. Error: "input (263 tokens) is too large to process. increase the physical batch size (current batch size: 256)". Fix: batch-size 256→1024 in model_registry.py (2048 caused OOM: reranker RSS 4.6→7.9GB on 22GB total). 1500ch truncation in _rerank_score() + reranker_score() as belt-and-suspenders (600tok, safely under 1024). Multiple regression tests confirm 0 reranker 500 errors.'
  status: resolved
- id: nd13-02
  detail: 'Reranker launch bug: _launch_reranker() used podman exec -d without killing old process. Old process held port 8080 with stale config (ctx 2048, batch 256). Fix: add pkill -f "reranking.*8080" before launch. Also increased ctx-size 2048→4096 for headroom. Verified working across multiple test cycles.'
  status: resolved
- id: nd13-03
  detail: 'Batch NLI HTTP 500 (60 facts): evidence 500ch×60=30Kch (7500tok) + source 2000ch (500tok) + max_tokens 796 = ~8800tok > ctx 8192. Fix: evidence→context_limit(400) sandwich (200+200), source→context_limit(1000), plus token-based dynamic batch splitting with MAX_BUDGET=7373 (90% of 8192). Web-verified: "Lost in the Middle" (TACL 2024), EMNLP 2025, ACL 2024 all confirm sandwich+shorter is best. Dynamic batching = industry standard (vLLM, TGI, SGLang). Verified 0 errors in 40-turn regression (165 facts).'
  status: resolved
- id: nd13-04
  detail: '_get_unprocessed_turns() filtered only pipeline_state=scanned, missing 5015 pending turns. Fix: IN (scanned, pending). Also added --large-only CLI flag (filters LENGTH(user_turn)>2000 OR LENGTH(text)>2000) for separating long identity docs. Verified in regression test — extract processes pending turns correctly.'
  status: resolved
- id: nd13-05
  detail: '40-turn regression test (v4, context_limit(evidence,400) + token-budget batch splitting): rc=0, 0 reranker HTTP 500, 0 batch NLI errors. 23 turns processed, 165 new facts. Total DB: 965 facts. ~3h 50min wall time (4-core ARM load avg 6.5). All fixes confirmed production-ready.'
  status: resolved
- id: nd13-06
  detail: 'Swap analysis: OCI Block Storage (SSD network-attached, not HDD), swappiness=10, 2.9/4GB used despite low swappiness → genuine RAM pressure. batch-size 2048 caused OOM risk (reranker RSS 7.9GB). batch-size 1024 is safe sweet spot (RSS 4.6GB, available >4GB after reclaim). Verified stable through 4h regression test under sustained load.'
  status: resolved
known_issues:
- id: nki12-01
  detail: 'copilot CLI 1.0.68: keychain 없는 Linux 환경에서 plaintext token 저장 fallback이 동작 안 함. Login succeeds but token not saved. 해결: COPILOT_GITHUB_TOKEN env var.'
  resolved: false
- id: nki12-02
  detail: 'OpenRouter free model (:free)은 유료 키($10+)로 호출해도 cost=$0이지만 RPM 20 제한은 키 등급과 무관하게 동일. 15RPM proxy로 대응.'
  resolved: false
- text: 14B Q4 (Qwen3-14B-Q4_K_M) produces GOOD quality extraction predicates WITHOUT Golden Set on 760-char mixed EN/KR turns.
    GS adds 3x prompt tokens without clear quality benefit on this test. Needs evaluation on longer/harder turns.
  resolved: false
- text: '30B MoE (Qwen3-Coder-30B-A3B-Q4_K_M, 18G) slot deadlock after first request: cause is MemoryMax 22G too tight. Fixed
    at 23G + ctx 4096 + cache-ram 512. 14B Q4 is more stable alternative.'
  resolved: false
- text: 48 stuck turns from earlier session — enrich.py processing them. Status unknown.
  resolved: false
- text: 'CRITICAL: exa_mcp.py + context7_mcp.py _read_message()/_send_message() modified for JSON-line compat. If update_handover.py
    runs before session writes to handover.yaml, completed_log and decisions may get overwritten. Always check handover.yaml
    after session start.'
  resolved: false
- text: 'day-verifier-b (qwen2.5-coder-7b Q4_K_M, :8083): 등록만 되어 있고 day_verify.py에서 실제 호출 없음 (single model port 8082만 사용).
    Legacy dead config. 제거해도 무방하지만 유지해도 무해. 문제 생기면 llm_client/__init__.py, model_registry.py에서 제거.'
  resolved: false
- text: '(Deprecated) Gemini API SSL blocked from Oracle Cloud no longer blocks connections from gemini_openai.py — proxy
    uses raw SSL sockets with hardcoded IPs (bypasses /etc/hosts and any DNS-level blocks). Native Gemini protocol (proxies/gemini.py)
    also works independently. Reference: original issue nki05-01 now resolved for both proxies.'
  resolved: false
- text: 'devforge-mcp MCP tools (action_write, action_poll_results) require container restart for FastMCP tool registry update.
    Does not auto-reload on file change. Restart command: systemctl --user restart container-devforge-mcp.'
  resolved: false
- text: 'extraction: equals predicate still appears 1/turn even with Golden Set (30B MoE, PARALLEL=1). May need stronger GBNF
    constraints or prompt refinement.'
  resolved: false
- id: sys-day-extract-re-export
  detail: Backward compat for nli_compare.py, extract_compare.py. Now FREE. These tests work but test different prompts than
    before.
  resolved: false
- text: 'Gemini API SSL blocked from Oracle Cloud: all gemini/* models fail with TLSV1_ALERT_INTERNAL_ERROR. DeepSeek/OpenAI/OpenRouter
    all work. Proxy/rotation infrastructure for Gemini unusable from this server.'
  resolved: false
- id: nki01-01
  detail: 이전 cycle 49 day_verify facts NLI/reranker 없이 force-pass. reranker_recover.py 기본 조건에 source='day_verify' 포함 여부 확인
    필요. 미포함 시 SQL 일괄 UPDATE 필요.
  resolved: false
- id: nki01-02
  detail: cli.py status --json models.pod-a = ["?", "?", "?"]. Pod A /v1/models 응답 문제 지속. Pod A 기능상 정상 작동 중.
  resolved: false
- id: nki01-03
  detail: turns 테이블에 updated_at 컬럼 없음. pipeline_state 변경 추적 어려움. (created_at만 존재)
  resolved: false
- id: nki01-04
  detail: CONTRADICTION=0 미해결. 신규 GSAR COMPLEMENTARY 4-way NLI 첫 결과 확인 후 재평가 필요.
  resolved: false
- id: nki02-01
  detail: OCI root 확장 미완료. /etc/fstab 정리 완료, 백업 완료. Pod A는 정상 기동 중 (devforge-net 브릿지), Pod B 복구 필요.
  resolved: false
- text: OpenCode not yet fully tested in TUI mode (only headless opencode run verified). MCP server connection via opencode
    mcp add not tested with devforge-mcp container. OpenCode config at ~/.config/opencode/opencode.json uses provider.openai.baseURL
    pointing to local proxy.
  resolved: false
- text: Phase 1 test scripts in /tmp/ (test_30b_golden.py, test_14b_golden.py, test_14b_feedback.py) — not committed. One-shot
    test scripts, not production code.
  resolved: false
- text: 'Phase 1 test scripts: scripts/lib/test_runner.py (TestRunner framework) + scripts/tests/test_phase1_14b.py (14B Q4
    GS comparison). /tmp/ scripts are legacy — not committed.'
  resolved: false
- text: PostgreSQL shared_buffers 512MB — applied via restart. Ok.
  resolved: false
- text: 'Slot deadlock watchdog is reactive not preventive: --slot-prompt-similarity 0 reduces but doesn''t eliminate risk.
    If deadlock persists after Pod B restart, need different cache-reuse strategy (--cache-reuse 0 as last resort).'
  resolved: false
- id: strict-xplore-removed
  detail: 3 experimental test files (*_7b_vs_9b*) import FREE variants as aliases. Body still uses old names. Verify prompt
    equivalence.
  resolved: false
- text: 'Veritas single-batch predicate 간섭 리스크: MiniCheck 원 설계는 (document, single_claim) → binary. _binary_nli_batch에서 모든
    predicate를 한 prompt에 배치 전송. Claims 간 attention 간섭 가능성 있음. max_new_tokens=1 + numbered answer format으로 독립 평가 유도 중. 정확도
    문제 발견 시 각 predicate 개별 호출로 분리 (latency N배 증가).'
  resolved: false
- text: 'Reranker --batch-size 1024 on 22GB OCI ARM: RSS ~4.6GB, safe. Swap 73% used (2.9/4GB) despite swappiness=10, indicating genuine RAM pressure. Do not increase batch-size beyond 1024 without verifying free memory >6GB.'
  resolved: false
completed_log:
- 'Reranker HTTP 500 244→0 fix: triple-layer (batch-size 1024, ctx 4096, pkill+relaunch, 1500ch truncation). Batch NLI overflow fix: evidence 500→300ch, source 2000→1000ch. --large-only CLI flag. pipeline_state scanned→pending. 40-turn regression verified: 0 errors, 612 total facts.'
