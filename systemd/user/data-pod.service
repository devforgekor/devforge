# Automatically generated by /usr/lib/systemd/user-generators/podman-user-generator
# 
[Unit]
Wants=podman-user-wait-network-online.service
After=podman-user-wait-network-online.service
Description=Podman pod data-pod (Quadlet)
Wants=network-online.target
After=network-online.target
SourcePath=/home/opc/.config/containers/systemd/pod-data-pod.pod
RequiresMountsFor=%t/containers
Wants=container-postgres.service
Before=container-postgres.service

[X-Pod]
PodName=data-pod
ServiceName=data-pod
Network=devforge-net
PodmanArgs=--memory=2g
ExitPolicy=stop

[Service]
Restart=on-failure
RestartSec=3s
TimeoutStopSec=70
SyslogIdentifier=%N
ExecStart=/usr/bin/podman pod start data-pod
ExecStop=/usr/bin/podman pod stop --ignore --time=10 data-pod
ExecStopPost=/usr/bin/podman pod rm --ignore --force data-pod
ExecStartPre=/usr/bin/podman pod create --infra-conmon-pidfile=%t/%N.pid --replace --exit-policy continue --network devforge-net --infra-name data-pod-infra --name data-pod --memory=2g
Environment=PODMAN_SYSTEMD_UNIT=%n
Type=forking
PIDFile=%t/%N.pid

[Install]
WantedBy=default.target
