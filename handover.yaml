last_checkpoint:
  time: '2026-06-26T05:30:00.000000+00:00'
  summary: "OpenCode AGENTS.md rules research, MCP Tools section added to llm-agent-rule.md"
  total_files: 2
  recent_files:
    source:
      - /home/opc/llm-agent-rule.md (MODIFIED)
    config:
      - /home/opc/.claude/CLAUDE.md (REVERTED)
    data: []
    docs: []
  git:
    last_commit: e641b5c
    summary: "MCP Tools usage table added to agent rules"
decisions:
- '{''id'': ''nd05-01'', ''detail'': ''Slot deadlock auto-detection in watchdog: _check_slot_deadlocks() in __init__.py, SLOT_STUCK_THRESHOLD=3 in state.py, recover_slot_deadlock() in recovery.py. llama-server PR #22083 (--slot-prompt-similarity 0) workaround in pod-b-entrypoint.sh. watchdog restarts Pod B on ≥2 processing slots stuck for ≥3 cycles.''}'
- '{''id'': ''nd05-02'', ''detail'': ''Pod A port publishing failure (pasta crash): root cause = pasta ASSERTION fail + flow_hash crash (GitHub #19405, #26073). Resolution: slirp4netns. Trade-off: 1/7 throughput but Pod A only runs reranker/polish/tiny (latency-insensitive). Applied via pod-a.pod Network=slirp4netns.''}'
- '{''id'': ''nd05-03'', ''detail'': ''MCP --host 127.0.0.1 default caused rootlessport "Connection reset by peer": slirp4netns routes through tap0(10.0.2.x), MCP must bind 0.0.0.0. Fixed mcp_entrypoint.sh --host 0.0.0.0.''}'
- '{''id'': ''nd05-04'', ''detail'': ''Stale model references cleanup: 5 files had outdated 14B/f16/Q4 comments. day-verifier was mistakenly changed to 9B Q8 → user caught it ("9b는 enricher 잖아") → reverted to Qwen2.5-Coder-7B Q8. Rule: port map comments in pod_manager.py are SSOT, pod-b-entrypoint.sh comments are mirror.''}'
- '{''id'': ''nd05-05'', ''detail'': ''PostgreSQL shared_buffers 4GB→512MB: restarted, verified effect. DB 668MB 기준 적정값. 동적 조정 불필요 확인 (PG가 자체 buffer 관리).''}'
- '{''id'': ''nd05-06'', ''detail'': ''Aider Qwen wrapper: aider_qwen.py replaces gemini wrapper. Gemini SSL blocked (Oracle Cloud TLSV1_ALERT_INTERNAL_ERROR). OpenRouter Qwen3 Coder 480B (:free) via QWEN_480B_API_KEY. Rule injection: infrastructure.md + llm-common-rule.md + llm-agent-rule.md auto-attached via --read. alias aider='python3 /opt/projects/server/scripts/aider_qwen.py' in .bashrc.''}'
- '{''id'': ''nd05-07'', ''detail'': ''Aider real-world test: generated pytest for analyze_exit_code() with 30 parametrized test cases, all passed. Path handling correct (unlike DeepSeek direct mode). $0.0019 cost. Qwen3 Coder 480B is competent for code generation. Free tier rate-limited but usable with pauses.''}'
- '{''id'': ''nd06-01'', ''detail'': ''OpenCode 1.17.11 installed globally via npm. Node.js v24.15.0. Works with Anthropic proxy (DeepSeek V4 Flash via ANTHROPIC_BASE_URL). read-only and tool-using tasks verified. MCP native support confirmed (opencode mcp add --url/--env).''}'
- '{''id'': ''nd06-02'', ''detail'': ''Gemini OpenAI-compatible proxy (gemini_openai.py): FastAPI on 127.0.0.1:4431. OpenAI /v1/chat/completions → Gemini native protocol translation. KeyRotator integration with 6 keys from secrets.env (round-robin, auto-backoff on 429). Bypasses /etc/hosts redirect via hardcoded Google IPs (same pattern as proxies/gemini.py). Independent state file (~/.cache/devforge/gemini_openai_rotator_state.json) — does NOT interfere with gemini.py (port 4430). Verified: non-streaming, streaming SSE, /v1/models all pass. systemd user service enabled (boot auto-start).''}'
known_issues:
- '{''id'': ''nki05-01'', ''detail'': ''Gemini API SSL blocked from Oracle Cloud: all gemini/* models fail with TLSV1_ALERT_INTERNAL_ERROR. DeepSeek/OpenAI/OpenRouter all work. Proxy/rotation infrastructure for Gemini unusable from this server.''}'
- '{''id'': ''nki05-02'', ''detail'': ''Slot deadlock watchdog is reactive not preventive: --slot-prompt-similarity 0 reduces but doesn't eliminate risk. If deadlock persists after Pod B restart, need different cache-reuse strategy (--cache-reuse 0 as last resort).''}'
- '{''id'': ''nki06-01'', ''detail'': ''(Deprecated) Gemini API SSL blocked from Oracle Cloud no longer blocks connections from gemini_openai.py — proxy uses raw SSL sockets with hardcoded IPs (bypasses /etc/hosts and any DNS-level blocks). Native Gemini protocol (proxies/gemini.py) also works independently. Reference: original issue nki05-01 now resolved for both proxies.''}'
- '{''id'': ''nki06-02'', ''detail'': ''OpenCode not yet fully tested in TUI mode (only headless opencode run verified). MCP server connection via opencode mcp add not tested with devforge-mcp container. OpenCode config at ~/.config/opencode/opencode.json uses provider.openai.baseURL pointing to local proxy.''}'
- '{''id'': ''nki04-01'', ''detail'': ''PostgreSQL shared_buffers 512MB — applied via restart. Ok.''}'
- '{''id'': ''nki04-02'', ''detail'': ''48 stuck turns from earlier session — enrich.py processing them. Status unknown.''}'
completed_log:
- '{''session 2026-06-25T07:48'': ''OCI root extension. Services stopped, /var/log+tmp backed up, fstab edited. Blocked at LV removal.''}'
- '{''session 2026-06-25T11:05'': ''PostgreSQL tuning (effective_io_concurrency=200, effective_cache_size=1GB, shared_buffers=512MB). day-cycle.timer removed from 5 files. extract.py deadlock fixed (recover_8082 missing lock.release). Server perf analysis completed (no-mmap keep, netdata fine, zram keep). systemctl kill stuck day_cycle. 48 scanned turns pending watchdog resume.''}'
- '{''session 2026-06-25T12:30'': ''Slot deadlock watchdog auto-detection + recovery. Pod A slirp4netns. MCP --host 0.0.0.0. Stale model refs cleanup (14B→8B, f16→Q8). PostgreSQL shared_buffers restart. --slot-prompt-similarity 0. Aider Qwen wrapper. Gemini SSL blocked.''}'
- '{''session 2026-06-26T01:50'': ''OpenCode 1.17.11 npm install + verification (Anthropic proxy / DeepSeek V4 Flash). Gemini OpenAI-compatible proxy (gemini_openai.py) built with FastAPI + raw SSL sockets + KeyRotator (6 keys round-robin). systemd service gemini-openai-proxy.service on port 4431. Verified non-streaming, streaming SSE, /v1/models. OpenCode config points to local proxy. Aider still available but OpenCode tested as alternative.''}'
- '{''session 2026-06-26T05:30'': ''OpenCode AGENTS.md rules research. MCP installation confirmed at /home/opc/.claude/mcp.json (13 servers). Added MCP Tools usage table to llm-agent-rule.md (shared by Claude Code + OpenCode). brave-search simplified (no Claude/OpenCode divergence).''}'
