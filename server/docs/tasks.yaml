meta:
  location: /opt/projects/server/docs/tasks.yaml
  purpose: "Shared Kanban task tracker for all AI agents on DevForge server."
  language: "English only (machine-readable)."
  related_files:
    - /opt/projects/server/CLAUDE.yaml
    - /opt/projects/server/handover.yaml
    - /opt/projects/server/blueprint.yaml
    - /opt/projects/server/docs/design.md
    - /opt/projects/server/docs/phases.md
    - /opt/projects/server/_archive/README.md
  must_rules:
    - "in_progress: exactly 1 task or null. Never leave a completed task in in_progress."
    - "todo: ordered by priority. Add new tasks to the end."
    - "blocked: keep blocked items separate from todo."
    - "done: keep at most 10 recent items. Older items exist only in worklog DB."
  done_checklist:
    - "Move in_progress -> done (insert at top of done list)."
    - "If todo is non-empty: promote todo[0] -> in_progress."
    - "If a task becomes blocked, move it to blocked and record the blocker."
    - "Log to DB: python3 /opt/projects/server/scripts/cli.py worklog add '<title>' '<summary>' --agent '<agent>' --model '<model>' --tags '<tags>'"
    - "If decisions were made or issues found: update handover.yaml."
  add_checklist:
    - "Append new task to todo with date and optional tags."

kanban_labels:
  todo: "To Do"
  in_progress: "In Progress"
  blocked: "Blocked"
  done: "Done"

in_progress: null

todo: []

blocked: []

done:
  - "Copilot statusline quota tracking — statusline now parses Copilot Student Benefit quota (entitlementRequests/usedRequests/remainingPercentage) from live payload, falls back to DevForge API when unavailable, and shows context warnings with color-coded progress bar (yellow/orange/red)."
  - "Copilot statusline format — the Copilot footer now shows a compact progress bar, percent, used/total tokens, and remaining tokens, with model names and request labels removed."
  - "Copilot statusline fallback — copilot-statusline.py now reads the newest useful ~/.copilot/session-state/*/events.jsonl snapshot when no live payload is provided, preferring full used/limit snapshots and falling back to used-only snapshots."
  - "Show overall token percentage — the MOTD [TOKENS] block now shows only `copilot: 56%` style usage based on total token volume."
  - "Simplify MOTD token block — the generated MOTD now shows only a single `copilot` percentage line for overall token usage."
  - "Restore visible token status — Gemini token usage is now ingested from raw session logs, duplicate rows merge token metadata, and both MOTD and SessionStart show live token totals."
  - "Restore MOTD token summary — the generated MOTD now shows current-session and total token usage instead of a 7d-only token line."
  - "Restore MOTD token info — re-added the [TOKENS] section to the generated MOTD and state snapshot; it now reads from turns.meta.tokens when available."
  - "Gemini shell-bound exit 2026-05-16 — restored the foreground Gemini launcher so it exits with the shell/session, matching the original install-time behavior."
  - "Reboot validation 2026-05-16 — all containers healthy, DB intact (884 turns), 12 timers active, handover known_issues cleared."
  - "/opt/workspace reference audit and cleanup — searched entire codebase, confirmed zero active code references, updated common-main.md Storage entry to out-of-scope."
  - "Decision table migration — added a dedicated obs_dec table keyed by turn_id, wired save_memory to write decision rows when meta.type = decision, updated schema/docs/stats, and backfilled 10 conservative historical decisions."
  - "Qwen worker 2차 로직 감사 — 8개 버그 발견/수정: (1) has_linking stale after backfill ctx refresh, (2) agent 미검증 SQL injection 위험, (3) call_qwen attempt 표기 오류, (4) prompt 레이블 7/30일 불일치, (5) execute_ingestions dead code 제거 + import 정리, (6) _ingest_sessions retry backoff 누락, (7) gather_context canonical set 불일치(deepseek 누락), (8) _validate_linkages int/str 타입 불일치. 단위 테스트 15/15 통과."
  - "DeepSeek diagnostics — scripts/deepseek_web.py now supports --dump-dir to save blocked-page HTML, screenshots, and JSON metadata for diagnosis; verified against the CloudFront bot challenge page"
  - "DeepSeek web CLI — scripts/deepseek_web.py added as an isolated Playwright-based browser CLI, Playwright and Chromium installed locally, browser runtime dependencies installed on the host, local smoke test on a data URL passed"
