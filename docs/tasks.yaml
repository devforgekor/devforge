tasks:
- id: 5phase-experiment
  status: pending
  priority: P0
  title: 2x2+baseline 5-Phase 실험 실행
  description: >
    Phase 0 (기준선:S✗R✗F✗) → 1 (구조:R✗F✗) → 2 (구조+R:F✗) → 3 (구조+F:R✗) → 4 (구조+R+F).
    실험 스냅샷 5개 준비 완료. P-R-J 모델 교체도 고려.
    대상: 최종 LLM 선택까지 확정 후 실행 예정.
    사용법: cd scripts && nohup python3 experiment_runner.py > /tmp/experiment_run.log 2>&1 &
  notes:
    - "느려지는 원인 (7B ~2.3 t/s, 27B OOM 리스크) 분석 및 최적화 후 실행"
    - "저녁 시간대 (KST 20:00+) 실행 예정"
    - "MCP 모델 7B Q8_0 ~31s. Qwen3-4B(2.7GB) 변경 시 extract 속도 개선 가능"

- id: prompt-optimization
  status: pending
  priority: P1
  title: LLM system prompt 비대화 원인 분석 및 최적화
  description: >
    SYS_P/R/J/V27 등 system prompt가 RUBRIC(600자) + scale + evidence
    설명으로 누적되어 비대해짐. 각 system prompt의 실제 토큰 수 측정 및
    최적화 불필요한 중복/장황한 설명 제거.

- id: mcp-model-optimization
  status: pending
  priority: P1
  title: MCP extract 모델 최적화 (속도/파싱)
  description: >
    7B Q8_0 MCP ~31s. Qwen3-4B(2.7GB, Q8_0 ~4.3GB)로 교체 시
    로드 시간 단축 및 parse_failed 개선 가능.
    extract 응답 JSON 포맷 정확도 문제 있음 (parse_failed 다수).

- id: j-model-replacement
  status: pending
  priority: P1
  title: P-R-J J(Selene) 대체 모델 선택
  description: >
    Selene JSON 출력 불량 (feedback loop 차단).
    후보: 7B Q8_0, 14B Q8_0, Qwen3-Coder-30B Q3_K_M(P와 공유).
    P-R-J 모델 조합 최종 결정 후 스냅샷 재구성 필요.

- id: memory-reclaim-recovery
  status: completed
  priority: P0
  title: 메모리 회수 + 자율 복구 로직 구현
  description: >
    kill_all() 후 _reclaim_memory() 추가 (sync+15s 대기).
    recover_and_restart(attempt): 3단계 에스컬레이션 (mild→aggressive→minimal).
    실험러너 및 prj_cycle.py에 적용 완료.

- id: transform-refactor
  status: completed
  priority: P1
  title: transform_prj.py 독립 플래그 리팩터
  description: >
    기존 --phase N 방식 → --structural / --rubric-off/on / --feedback-off/on
    조합 가능으로 변경. Phase 0-4 스냅샷 준비 완료.
    버그: \n vs 실제 개행문자 불일치 수정.
